# EDGE-DS: Personal → Corporate Claude Account Migration Handoff

Prepared 2026-07-23, last updated 2026-08-07 (Paper built and corrected same day: token migration including the new `Semantic/Elevation/0-24` tier, legacy cleanup, Gallery + Documentation, zero-edit inheritance confirmed on Drawer and Dialog, plus a same-day correction pass that fixed residual MUI-palette bindings a first sweep wrongly scoped out and rebuilt both Top Headers to match the verified Link reference — surfaced a new file-wide Top Header naming-inconsistency backlog item, see §4). Read this document first in the new corporate Cowork session, before touching any component work.

## 1. How to actually migrate this

Anthropic does not support exporting or importing Cowork projects between accounts, even with the same email. There is no "export project" button, and a personal-account data export cannot be re-imported anywhere. The conversation history genuinely cannot be carried over.

What *does* carry over cleanly, because it never lived in the account, it lives on your Mac:

- The project folder itself (`/Users/IgorMenezes/AI Projects/EDGE-DS`), including the full git history, uncommitted working-tree changes, `docs/`, `skills/`, and the `.env` file.
- Your Figma file, tokens, and components (these live in Figma, not in Claude, and are untouched by any of this).

So the migration is really two steps:
1. In the corporate account's Cowork session, select/mount the same `EDGE-DS` folder you already use. All files, git history, and prior commits are immediately there, nothing to copy.
2. Paste this document as your first message in that session (or just say "read the migration handoff doc in the project root"). It replaces the lost conversation history and auto-memory by giving the new session the equivalent context in one shot: process, standards, what's locked, what's mid-flight, and the gotchas already paid for.

This is the standard workaround people use for this exact problem (a "setup instruction document" fed to the fresh session) since there's no first-party alternative.

## 2. Project overview

EDGE Design System (EDGE-DS): the single source of truth for UX/UI across EDGE corporate products, primarily the main platform and the **EDGE Pay Tool**. Clean, minimalist SaaS aesthetic — soft container shadows, consistent rounded corners, high data density without crowding (built for financial/transactional screens).

**Stack:** Next.js (App Router) + React + TypeScript, Material UI (MUI) as the component base, Emotion for styling, Tailwind config present for token consumption, Supabase for auth/data. Figma (Variables, multi-mode) is the design source of truth; `src/theme/brandTheme.ts` is the single source of truth for the web theme.

**Correction, 2026-08-06:** the line below was previously documented as an EDGE-DS project hard rule and has been enforced as one in every component pass so far (every audit doc self-reports a "zero em dashes" scan). Igor has clarified this is actually a personal writing-style preference for text Claude drafts on his behalf (messages, emails), not a rule for this project. Going forward, do not scan for or enforce em-dash removal in Figma text, audit docs, code, or commit messages for EDGE-DS. Existing em dashes already in the codebase or docs (including this file) do not need cleanup.

**Repo:** `https://github.com/igormzs/edge-ds.git`, branch `main`.

## 3. Documentation methodology (read before touching any component)

Two canonical references already in the repo, read these instead of re-deriving the pattern (as of 2026-07-28, these superseded the older `docs/DOCUMENTATION_STANDARDS.md` / `EDGE-DS-Component-Migration-Playbook.md` / `EDGE-DS Documentation Pattern.md` / `skills/*.md` files, now archived under `docs/archive/process/`):

- **`docs/web-component-page-pattern.md`** — the ratified web styleguide page pattern: header/badge conventions, subcomponent tab navigation, the matrix-card Visual Preview layout, typography/content-hierarchy rules, theme-level spacing overrides, and the exact section backbone order. Ratified against the refactored `<Switcher>` page.
- **`docs/figma-component-structure.md`** — the ratified Figma-side standard: the Two-Frame Canvas Architecture (Component Gallery + Documentation), master frame/section-wrapper anatomy, typography token table, page order, the token migration workflow (Live Inspection → Gap Analysis → Token Creation → Automated Rebinding → Visual Verification), and a Figma-automation gotcha list. Any new component doc must follow both of these exactly.
- **Figma master template** — a true Figma COMPONENT, `Component Doc - EDGE-DS` (node `751:165195`, page `📘 Component Documentation Template` / `348:63053`, file `EDGE Design System - New`, fileKey `fLQNXhHQhKBZzWnJGtUcwn`). Every new component doc page should **instantiate this**, not get rebuilt from scratch. It has 4 slotted section cards plus a Key Props table with a duplicable row slot, plus optional Header/Footer note slots per section.

General working pattern established across every component so far: audit Figma geometry/tokens → fix real bugs found (not silently, always flagged) → create/bind design tokens → build or correct the web styleguide page (`src/app/styleguide/...`) → build the Figma documentation canvas from the master template → run a 1:1 parity audit between the two → commit locally (pushes are held for you to do manually, or blocked by sandbox network restrictions in Claude's own bash sandbox — never a problem on your real machine).

**Process change, 2026-08-05, applies to every component from Drawer onward: the "build or correct the web styleguide page" step above is paused project-wide, pending a planned migration to Storybook.** Reasoning: the custom `src/app/styleguide/[component]/page.tsx` pages are mostly hand-built layout scaffolding (matrix-card previews, Key Props tables, tab nav) that Storybook's Controls/autodocs generate automatically from component types and `argTypes`, so building them now is throwaway work. What does NOT pause: wiring the real `brandTheme.ts` theme override for the component, and writing the Figma-vs-web parity audit (`docs/{Component}_Figma_Web_Audit.md`), both stay in the normal per-component pass, since that's real product code and real bug-finding, not documentation. Anatomy/Token/Usage/Accessibility prose should still be written in plain terms (it copy-pastes into Storybook's MDX docs later). Once Storybook is actually set up, migration is expected to run one component at a time alongside the existing `/styleguide` app, not as a blocking cutover, and the paused page-build step resumes in Storybook's format instead of the old custom-page format. Nobody besides the account owner uses the deployed `/styleguide` site, so there is no external audience cost to this pause.

## 4. Component status (what's locked vs. what's open)

| Component | Status | Notes |
|---|---|---|
| Button | Locked (2026-07-15, corrected 2026-07-16) | Tokens mapped, real State-variant axis used for Hover/Focus, doc page built from master template |
| Chip | Locked/complete (2026-07-09) | 45 tokens created + bound; 3 intentional gaps deferred (disabled-state colors, no focus ring, no outlined-hover fill) — see `Chip_Context_Progress_Report.md` |
| Breadcrumbs | Locked/complete | Website + tokens done; Figma canvas rebuilt from master template (2026-07-15) |
| Pagination | Locked/complete | Website + tokens done; Figma canvas from master template; Hover state is a flagged manual approximation (no real Hover variant exists on `PaginationItem`) |
| Button Group | Locked/complete | Corner-radius bug fixed, dead fill removed; 6 of 14 divider tokens are literal values, not aliases (flagged architecture gap, not a bug) |
| Accordion | Locked (2026-07-17) | Filters variant now built from real `<Accordion>` shell + real `<Chip>` instances, no bespoke components |
| Alert | Locked (2026-07-17) | All 4 master slots relabeled to Alert's own categories (Severities/Variants/States/Filled-All) |
| Alert token discrepancy | Resolved (2026-07-20) | Figma bg/text tokens were pinned to raw palette steps while Web computed them dynamically; fixed by writing Figma's literal computed values; also fixed a Warning text/icon color collision. Dark-mode parity for Alert was **not** covered — flag if asked |
| Autocomplete | Built (2026-07-20), long troubleshooting history | Sizing slot has reset to empty 4+ times on file reload (root cause: uncommitted slot content is live-runtime-only until a structural fix persists it — a durable fix requires detaching the whole top-level instance, which needs your manual approval/action in Figma). As of 2026-07-21 it reloaded correctly. If it happens again, the rebuild script is already known/recorded, don't re-diagnose from scratch |
| Backdrop | **Locked/complete (2026-07-31 Figma, web wired 2026-08-05 update ↦ actually confirmed via commit `1a81a16`, 2026-07-31)** | Figma Style×Visibility variant set built and tokens bound (`Components/Backdrop/Fill/{Default,Blur,Inverted}`), Component Gallery frame built, Documentation frame upgraded to current standard — see `docs/Backdrop_Figma_Web_Audit.md`. Web side (`brandTheme.ts` `MuiBackdrop` overrides + styleguide doc page) implemented in commit `1a81a16 feat(backdrop): implement web documentation page and brandTheme overrides`. No longer a pending item; superseded note below corrects the prior "Figma master is still a raw MUI import" line, that gap is closed on both sides. |
| Switcher | Built (2026-07-21/22), further synced 2026-07-30 | Parity audit found and fixed 4 real Figma token/binding bugs (wrong Secondary color token, broken Primary opacity, knob color mismatch, Off-track token drift). 2026-07-22: Off vs Indeterminate lightness was intentionally flipped so Indeterminate reads lighter/undecided. Committed (`fcdfeec`), then further web-doc/variant sync in `67df07a` (2026-07-30, bundled with Button/ButtonGroup/Autocomplete/Alert) |
| Autocomplete | Further synced 2026-07-30 (`dfe74df`) | Full visual parity pass + downward dropdown behavior fix, on top of the 2026-07-20/21 build noted above |
| Form Control Label | New, not previously tracked (`0b061eb`, 2026-07-24) | "Dual" label placement added, both Figma and web |
| Drawer | **Figma-side locked/complete (2026-08-05)** | Single `COMPONENT`, no variant properties. Rebound `<Paper>` fill from raw `background/paper-elevation-16` to `Semantic/Surface/Paper` (also caught one instance drifted to `background/paper-elevation-1`, same color, fixed as a side effect). Scrim need confirmed already covered by `Semantic/Overlay/Scrim` via composition with the real, migrated `Backdrop` component, no new token created. Gallery + Documentation frames built, legacy frame archived. Web styleguide page intentionally **not built**, paused pending Storybook (see process-change note in §3). Full detail in `docs/Drawer_Figma_Web_Audit.md`. |
| FAB | **Locked/complete, closing review passed 2026-08-06 — Go** | Real `COMPONENT_SET`, 216 variants (`Form` x `Size` x `Color` x `State`, 9 colors), full grid, zero gaps except the disclosed `Fab.figma.tsx` `Color`-enum gap (see flagged gaps below). All fills bound to pre-existing `Components/Button/*`/`Semantic/*` tokens; one new token `Components/Fab/Shared/FocusRing` (aliasing `Semantic/Surface/Paper`, matching `Components/Switch/Knob/FocusRing`). `MuiFab` entry in `brandTheme.ts` carries the focus-visible ring override. `Fab.figma.tsx`'s `Circular`→`Round` drift fixed. **Now the parent of a second real master family, SpeedDial (see row below): reorganized into FAB's own Gallery/Documentation as a multi-master page, following the Switch/FormControlLabel/FormGroup precedent — SpeedDial no longer has its own page.** Closing review (2026-08-06): 386 instances page-wide resolved with zero unresolved, all 216 FAB variants 100% EDGE-bound with zero regression, `tsc`/`next build` clean, zero em dashes, final verdict **Go**. One open, non-blocking flag, see the page-disappearance anomaly below. Full detail in `docs/FAB_Figma_Web_Audit.md` (§7 color-gap closure, §8 SpeedDial merge, §9 closing review). |
| SpeedDial | **Reorganized as a FAB sub-family, closing review passed 2026-08-06 — Go** | No longer a standalone page, merged into `FAB - Component Gallery`/`FAB - Documentation` as a second `MASTER COMPONENT SET` section, per your instruction to model it as a FAB variant family. Both structural gaps from the original discovery pass stay closed: `<SpeedDial>` has a real open/closed axis (renamed **`State`→`Visibility`** during the closing pass, cascaded through every variant name and 6 prose/table mentions; FAB's own unrelated `State` axis was correctly left untouched), and `<SpeedDialItem>` is a real 3-variant set (property renamed **`Display`→`Content`**, values Icon/IconAndTitle/TitleOnly unchanged). Post-merge fixes from your review: SpeedDialItem's card-bounds bug fixed (a leftover 160px margin, cloned from a two-axis source that needed a row-label column, was pushing content outside the clipped card boundary), a Visual Preview sub-block added, the multi-master section added to the Documentation frame as well as the Gallery (per your feedback that it was Gallery-only), and labels repositioned outside the display container. **Token migration remains explicitly deferred**, real un-migrated raw MUI bindings remain (raw `background/paper-elevation-6`, `text/secondary`, `components/tooltip/fill`, literal white hex, ~46 bindings total per `docs/SpeedDial_Figma_Web_Audit.md` §2), residual counts reconfirmed in the closing review as pre-disclosed debt, not new regressions. No `MuiSpeedDial*` entry in `brandTheme.ts` yet. Full detail in `docs/SpeedDial_Figma_Web_Audit.md` (§7 reorg, §8 post-merge fixes, §9 closing review). |
| Icons | **Built 2026-08-06** | Structurally a hybrid, unlike every prior component: one real generic `<Icon>` `COMPONENT_SET` (`Size` x instance-swap, mirrors `SvgIcon`'s `fontSize` prop) plus 125 independent single-variant icon masters plugging into it. Gallery grouped into 6 real sections (the wrapper, Filled 87, Outlined 10 including `ErrorOutline`, Sharp 1, Custom/EDGE 22, Utility/Non-Production 5 for the `_hidden` placeholders), built using `layoutWrap: 'WRAP'` auto-layout for the first time on this project rather than hand-computed grid positions. **Token migration done, precisely scoped, not blanket-applied:** a full pass across all 125 icons (not just the earlier partial sample) found 16 icons with real, intentional non-uniform coloring (9 file-type/trend icons on real brand colors, 7 two-tone icons mixing colors), rebinding those would have been a regression; rebound only the 112 icons plus the wrapper's 4 variants that actually used `action/active`, to `Semantic/Icon/Default` (verified an exact value match first, no new token created). Two small reconciliation gaps flagged for design (`CleanInputs` Figma-only, `check`/`heart`/`star` web-only), plus a longer-term open question: whether icons should own a fixed fill at all vs. inheriting `currentColor`. No web code changes made or possible, icons render via static SVG/`@mui/icons-material` with zero runtime Figma coupling, confirmed by grep. Full detail in `docs/Icons_Figma_Web_Audit.md`. |
| Link | **Built 2026-08-06** | Real 18-variant `COMPONENT_SET` (`Color` x `Underline` x `State`), completed this pass from 14 variants: the 4 missing `Focused` variants (`Underline=Always`/`On Hover`, both colors) were cloned from their `Hovered` siblings, with `Underline=On Hover`'s clones deliberately mirroring `Hovered`'s underline visibility (keyboard focus should be at least as visible as mouse hover). A real omission was caught and fixed mid-build: the 2 pre-existing `Focused` variants carry a 2px focus-ring outline the 4 new clones initially lacked, found via screenshot verification, added to match. New `Components/Link/*` tier created (`Text/Primary` → `Brand/Primary/500`, `Text/Inherit` → `Semantic/Text/Primary`, the latter a disclosed near-match not an exact one), a deliberate deviation from the Drawer/Icons direct-to-Semantic precedent, justified because no correctly-named `Semantic/*` token exists yet for a link-text role, not because Link needed multiple states encapsulated. Rebound across all 18 variants, 3 consecutive clean residual scans. New `MuiLink` `brandTheme.ts` entry (none existed before), wired to the same two tokens. Only one real web usage site exists (`breadcrumbs/page.tsx`), needed no changes. Full detail in `docs/Link_Figma_Web_Audit.md`. |
| List (List, ListItem, ListSubheader) | **Built 2026-08-07** | Multi-master page, same pattern as Switch/FormControlLabel/FormGroup and FAB/SpeedDial: `<List>` (4 variants), `<ListItem>` (20 variants, bakes MUI's pre-v5 button-state responsibility directly onto itself), `<ListSubheader>` (single COMPONENT, no variant axis). All three already bound to raw MUI palette variables (not literal hex, an earlier discovery-pass bug misreported them as fully raw, self-caught and corrected before any rebinding happened). Rebound 53 fills to `Semantic/Text/{Primary,Secondary,Disabled}` plus 3 new `Components/List/ListItem/{Hover,Focus,Selected}` tokens (Selected mirrors `Brand/Primary/300`, confirmed as MUI's own primary-scoped selected value, not an EDGE deviation). Deleted a genuinely dead, invisible legacy fill (confirmed inert first). New `MuiList`/`MuiListItem`/`MuiListSubheader` `brandTheme.ts` entries, deliberately scoped rather than touching the existing global hover/focus/selected palette (that's an unrelated, pre-existing teal-tinted customization other components depend on). **Closed, as a byproduct, the previously-flagged Drawer nested-List-content gap** (see `docs/Drawer_Figma_Web_Audit.md` §8): Drawer's mockups compose live, non-detached instances of these same masters, so they inherited every rebind automatically, zero separate Drawer edits needed. Full detail in `docs/List_Figma_Web_Audit.md`. |
| Paper | **Built 2026-08-07, correction pass same day — Go** | Real 52-variant `COMPONENT_SET` (`Variant` x `Elevation` 0-24 x `Square`), already structurally complete at discovery — no missing variants, unlike Link. Confirmed by direct `mainComponent.parent.id` resolution (not assumed) that this is the exact shared master Drawer's 6 and Dialog's 8 internal `<Paper>` instances already compose internally. Rebound all 25 `background/paper-elevation-{0-24}` + `elevation/outlined` to the existing `Semantic/Surface/Paper`/`Semantic/Border/Default` precedents (52 + 2 mutations), and established a brand-new `Semantic/Elevation/1`-`24` Effect Style tier (24 new styles, 48 rebinds) — the first tier of its kind in this file, cloned 1:1 from the pre-existing `elevation/N` MUI-import styles since the web side never customizes `theme.shadows` either. Both Drawer and Dialog inherited the full migration automatically, zero edits to either component's own file (confirmed directly, same pattern as List→Drawer; closing updates added to both audit docs). Legacy `_Library`/Grid wrapper archived, including a dead 100-node "Instances" grid relic found sitting in the same footprint as the real set. New single-master `Paper - Component Gallery` (axis labels read off the set's real, non-sequential `1,0,2,3…24` grid order) and `Paper - Documentation` built from scratch; both pre-existing real-world mockups folded in as `REAL-WORLD USE CASE` sections. **Correction pass, same day:** an initial "clean" pre-commit sweep had found but wrongly scoped out 24 real MUI-palette-bound nodes inside the two mockups as "pre-existing, out of scope" — caught via Figma's own color-picker panel showing live `MUI palette` entries still resolvable on the page. 10 of the 24 rebound to exact/disclosed-near-match `Semantic/*`/`Brand/*` tokens (including the correct role-specific `Semantic/Icon/Default` for icon glyphs vs. `Semantic/Text/*` for text); 7 more unbound literal `#ffffff` fills (found separately, verified by context not blanket rule) bound to `Semantic/Surface/Paper`; 7 `action/hover` bindings on `<Skeleton>` instances deliberately left un-forced (no existing token matches its translucent-overlay value without a real regression) — flagged as `<Skeleton>`'s own future debt. Both Top Headers were also found wrong on diff against `Link`'s verified reference (Gallery header was missing its Logomark/Wordmark Stack entirely; Documentation header's logomark was a flat color instead of the real image asset, its wordmark used a stale literal `#079C9C` instead of the shipped `Brand/Primary/500`, and both headers had a literal `#262D33` background instead of the existing `Documentation/Header/BG` token) — rebuilt to match exactly, real logomark asset cloned directly from `Link` rather than approximated. A file-wide cross-page check found the background-token bug was Paper-specific (all 15 other completed pages already correct), but surfaced a separate, real, pre-existing Top Header naming inconsistency (hyphen vs. em dash) affecting roughly half the file — logged as its own flagged item below, not fixed. 3 consecutive clean residual passes confirm 0 unbound literals and 0 unexpected raw bindings remain, save the 7 disclosed `action/hover` residuals. `MuiPaper` `brandTheme.ts` entry deliberately skipped — both token groups resolve to values already matching MUI's stock defaults, mirroring FAB's own "zero overrides needed" finding. Full detail in `docs/Paper_Figma_Web_Audit.md` (§8 correction pass). |
| Progress (CircularProgress, LinearProgress) | **Built 2026-08-10, correction pass same day — Go** | Multi-master page (Switch/FormControlLabel and FAB/SpeedDial pattern): `<Progress> \| Linear` (9 variants, `Type` x `Color`, rebind only) and `<Progress> \| Circular` (6 → 12 variants, new `Type` axis added this pass, `Determinate`/`Indeterminate` x `Color` x `Size`). Circular's arc rebuilt as real `arcData` ELLIPSE geometry (Determinate = fixed 75% illustrative sweep, Indeterminate = an offset ~252° sweep with zero textual annotation, confirmed to match Linear's own pre-existing zero-annotation Indeterminate before building, not invented fresh) — replaces a hand-drawn `VECTOR` path found at discovery. Two self-caught label corrections: Circular's percentage label (found genuinely visible by default, not hidden as an earlier discovery pass wrongly reported) now reads `75%` on Determinate and is switched off on Indeterminate. Group A: 4 raw MUI-import bindings (`primary/main`, `text/primary`, `text/secondary`, `background/paper-elevation-0`) rebound to new `Components/Progress/*` tokens (87 mutations, 3 clean passes). Group B: 5 new status colors (Error/Warning/Info/Success/Neutral) added via a brand-new `Semantic/Status/{Status}/Main` tier plus matching `Components/Progress/{Status}/Track` tokens — **not** aliased to Button's existing status tokens, a deliberate choice so Progress establishes this tier cleanly on its own. **Correction pass, same day:** pre-commit verification found 4 of the 5 new `Semantic/Status/*/Main` tokens pointed directly at a raw, wholesale-imported Material color library (`material/colors` collection, 170 of 292 vars following exact stock Material naming) — a real §5.1 violation at the Semantic tier itself, caught before anything shipped. Fixed by creating 4 new `Brand/{Error,Warning,Info,Success}/500` primitives (mirroring `Brand/Primary/500`'s exact shape) and re-pointing all 5 Semantic tokens through them (Neutral re-pointed to the existing, previously-missed `Brand/Secondary/500`, not a new primitive) — zero resolved-value drift confirmed by re-resolving every chain. Both real-world mockups handled: the pre-existing mismatched `Progress: buffer` mockup (it actually showed Circular content) renamed to `Progress: circular loading card`; a new `Progress: linear buffer` mockup built (a CSV bulk-import card composing two real Buffer instances). Legacy `_Library`/Grid wrapper archived to `_Archive / Progress / 2026-08-10`, 3 consecutive clean instance-resolution passes before archiving. Final chrome sweep found and bound 74 unbound literal `#ffffff` fills inside the masters' own internal frames (same class as Paper's own correction-pass finding) plus re-caught 9 nested-instance label overrides that reverted to a raw binding after reparenting (the documented flaky nested-instance behavior, §7.13) — 3 consecutive clean passes after, only the same disclosed, un-forced `<Skeleton>` `action/hover` residuals remain (10, both mockups). Button-dependency baseline was skipped (token creation ran before the planned pre-migration screenshot, a real process miss) — substituted with `mainComponent`-resolution checks plus exact-hex-chain verification at every checkpoint, confirmed zero value change; documented as a real process note in the audit doc, not silently glossed over. `MuiCircularProgress`/`MuiLinearProgress` `brandTheme.ts` entries and Code Connect deliberately out of scope. Full detail in `docs/Progress_Figma_Web_Audit.md` (§7 correction pass). |
| Radio (Radio, FormControlLabel \| Radio, RadioGroup) | **Built 2026-08-10** | Multi-master family, same pattern as Switch and Checkbox: `<Radio>` (132 variants, `Checked` x `Size` incl. a real, no-MUI-equivalent **Large** x `Color` (7) x `State`, Disabled built only for `Color=Default`, same representative-color gap as Checkbox), `<FormControlLabel> \| Radio` (8 variants, confirmed structurally distinct from Switch's/Checkbox's own FCL masters, needed its own token pass), `<RadioGroup>` (4 variants, composes real `<FormLabel>`/`<FormControlLabel> \| Radio`/`<FormHelperText>` internally). 4 new tokens: `Components/Radio/Icon/{Default,Disabled}`, `Components/Radio/Label/{Default,Disabled}` — `Icon/Default` is a new **literal** (not an alias), the Default dot/ring role was judged a graphical/icon role, not a text role, so `Semantic/Text/Secondary` was rejected despite being close in value (this project's established name-driven-not-value-driven precedent, same reasoning as Link's and List's own rejections). All 6 checked-dot colors reused Progress's `Semantic/Status/*/Main` tier + `Brand/Primary/500` directly (exact matches, zero new color tokens), and all 14 hover/focus halo values reused Checkbox's existing `Components/Checkbox/Ripple/*` tier directly after a byte-for-byte value check confirmed they're the same underlying ripple mechanism — zero duplicate `Components/Radio/Ripple/*` tier created. Self-caught an axis-orientation mistake mid-build on the RadioGroup Gallery section (assumed Checkbox's row/column convention, a direct geometry check showed the real axes were transposed — `Disabled` is the real column axis, `Radios` count is the real row axis — fixed before the section was called done). Web side: new `MuiRadio` `brandTheme.ts` entry, but verified directly against `node_modules/@mui/material/Radio/Radio.js` that stock MUI already colors every checked-dot status and the Default/unchecked icon (`theme.palette.text.secondary`, already an exact `rgba(0,0,0,0.6)` match) correctly on its own, so the entry needed almost no color overrides; Hover/Focus deliberately kept as the same flat global `action.hover`/`action.focus` overlay `MuiCheckbox` already uses rather than reconstructing all 14 approved Ripple values in code, matching Checkbox's own existing simplification, disclosed not silent. No `MuiRadioGroup` entry needed — verified `RadioGroup.js` renders `<FormGroup role="radiogroup">` directly with no distinct styled wrapper, so the existing `MuiFormGroup` `gap: 8` override already applies. `MuiFormControlLabel` already existed from Switch's/Checkbox's earlier pass, confirmed to need no extension. Zero web usage existed anywhere before this pass (confirmed by grep), no styleguide page built per the ongoing Storybook pause. Full detail in `docs/Radio_Figma_Web_Audit.md`. |
| Rating (Star, Rating) | **Built 2026-08-11** | Structurally novel shape: `<Star>` (15 of 18 possible variants, `Size` x `Active` (False/Half/Full) x `Hovered`, the `Active=False,Hovered=true` combo missing for all 3 sizes, a real confirmed gap not fabricated) and `<Rating>` (6 variants, `Size` x `Disabled`, no `readOnly`/`precision` variant axis — confirmed each variant composes a fixed 5 real `<Star>` instances, not a switchable value). First component in this file with a **real cross-page master dependency**: `<Star>`'s icon glyphs (`StarSharp`, `StarHalfFilled`, `StarOutlineFilled`) physically live on the Icons page under `Master Container/Swatch`. A full 72-page/12,256-instance scan confirmed the only other usage anywhere is Icons' own self-catalog demo of `StarSharp` (4 instances) — not a foreign consumer — and that Rating's own raw bindings actually lived as per-instance overrides on Rating's own page, not on the shared masters (which were already correctly bound to `Semantic/Icon/Default` and left untouched). 2 new tokens: `Components/Rating/Star/Active` (literal `#FFB400`) and `Components/Rating/Star/Border` (literal `rgba(0,0,0,0.23)`, light-mode only — a real upstream dark-mode value (`rgba(255,255,255,0.23)`) was deliberately dropped for the first time, not merely absent, consistent with this palette collection being single-mode throughout). Also repaired 6 broken per-instance overrides (drifted off the `StarHalfFilled` master's own correct `Semantic/Icon/Default` binding) and stripped 25 confirmed-rendered white fill residuals (19 on `<Star>`, 6 genuinely local on `<Rating>` — the other 34 originally counted were inherited duplicates that cleared automatically once `<Star>` was fixed). Web side: unlike Radio's/FAB's "zero override needed" precedent, `MuiRating` genuinely needed 2 explicit color overrides after verifying directly against `node_modules/@mui/material/Rating/Rating.js` (v7.3.9) — stock MUI's own hardcoded root color (`#faaf00`) and empty-icon color (`theme.palette.action.disabled`, this app's opaque `colors.grey[500]`) both genuinely differ from the Figma-approved values; `size` needed no override, stock 18/24/30px already pixel-matches. Zero web usage existed anywhere before this pass. No styleguide page, per the ongoing Storybook pause. Full detail in `docs/Rating_Figma_Web_Audit.md`. |
| Typography (Typography, Typography_EDGE type scale) | **Built 2026-08-11** | Structurally unlike every prior pass: not a blank slate (already had a real, non-trivial `edgeTypography` scale and 22 files of existing usage before this started), and not one real structure but **two, both kept**: the real 25-variant `<Typography>` `COMPONENT_SET` (MUI-named `Variant` x `Gutter Bottom`; the 13th variant, originally recorded as `custom` and originally believed mis-wired to an unrelated `table/header` variable, was investigated further and found to be correctly bound all along — a real, established table/data-grid meta-text style with 436 file-wide consumers and canonical use on the Table page's own `<TableHead>`. Renamed to `table-label`, no `edgeTypography` key added yet since Table/Data Grid have no web implementation to anchor one to — see the flagged gaps below) and `Typography_EDGE`'s type-scale specimen sheet (EDGE-named, Palette-shaped, now 12 entries). **Most consequential finding: `<Typography>` has 121 real instances across 12 other pages** (Accordion, App Bar, Progress, Avatar, Forms, Timeline, Headings, Tree View, Data Grid, Table, Menu, Overview) — turned a documentation-only-looking extraction into a real component move, verified 121/121 byte-identical pre/post via duplicate-safety-net procedure. Two real, previously-undetected web-side bugs corrected, both with zero regression since neither had any live usage: (1) a naming mix-up where `edgeTypography.body-xs` actually held Figma's `caption` values while Figma's own real `body-xs` variable was a different value entirely — resolved by adding a new `caption` key (holding the old values, re-pointed the MUI `caption` alias to it, zero visual change for its 4 real usages) and correcting `body-xs` to its real value; (2) `h1`/`h3` MUI aliases pointed one full visual tier smaller than Figma's own `h1`/`h3` bindings (`heading-xl`/`heading-md` instead of `display-lg`/`heading-lg`) — confirmed zero usage anywhere, corrected to match Figma exactly per explicit instruction rather than assumed intentional, after considering and disclosing that semantic-HTML-level-vs-visual-size decoupling is a legitimate pattern this could have been. Also corrected `heading-lg`/`heading-xs`/`body-lg`'s own stored metrics to match Figma (all zero-usage, zero-regression), added a new `overline` key (pure addition), and pinned `buttonLabelSm`'s `lineHeight` explicitly so it didn't silently inherit `body-xs`'s corrected value as an unrequested side effect. No `MuiTypography` styleOverrides block exists beyond the alias mapping itself, unchanged. No styleguide page built (Storybook pause continues; a `foundations/palette`-style page is the closest existing precedent for how a future Typography styleguide page might look, not built this pass). Full detail in `docs/Typography_Figma_Web_Audit.md`. |
**Same-day post-build correction pass:** a visual review after the build was called "done" found 4 real gaps the build's own clean checks hadn't caught — a stray 3rd top-level frame (`Typography_EDGE` was never archived after its content was carried into the Gallery, still holding its original truncated pangrams; now archived, exactly 2 frames), a placeholder rectangle standing in for the real EDGE-DS header logomark (now cloned from Radio's finished header), a real Master Component Set grid overlap on just the `h1` row (rebuilt from real per-variant geometry), and ~17 raw hex chrome fills across both frames instead of bound tokens (403 fill instances rebound; 11 exact matches to existing `Documentation/*`/`Semantic/*`/`Brand/*` tokens, 4 new tokens created — `Documentation/Flag/Warning-{BG,Text}`, `Documentation/Surface/Example`, `Documentation/Token/Background` — plus the 98 pangram/heading-text instances rebound from raw pure-black to `Semantic/Text/Primary`). A Button/Chip/Checkbox comparison also caught and fixed a real header-wordmark size mismatch (16px/0.5px tracking vs. the correct 20px/0% standard all three references share). The apparent 4,284-instance count that briefly looked like a 35x undercount of the original 121 turned out to be a transient mid-operation `getInstancesAsync()` artifact — re-verified clean at 121 after settling, see §6 for the lesson. **A follow-up text-style-tokenization pass same day** took both frames from 25/131 and 0/184 text-node coverage to 100%/100%: confirmed the established convention (doc chrome reuses the EDGE Type Scale keys directly — `caption`/`heading-sm`/`heading-md`/`body-md`/`overline`/`body-xs` — same as Button/Chip/Checkbox), then created 3 new `Documentation/*`-only Text Styles (`Documentation/Wordmark`, `Documentation/Table/Header`, `Documentation/Table/Body`) for the two roles no EDGE Type Scale key covers — the header wordmark (consistently 0% tracking file-wide, vs. `heading-xs`'s -0.05px) and dense tabular/matrix content (no 12-13px table-appropriate key exists). No `brandTheme.ts` changes, these are documentation-tool-only styles. Full detail in `docs/Typography_Figma_Web_Audit.md` §8/§8a. |

**Reusable foundation, not just a Progress-scoped detail:** the new `Semantic/Status/{Error,Warning,Info,Success,Neutral}/Main` tier and its 4 new `Brand/{Error,Warning,Info,Success}/500` primitives (§7 of `docs/Progress_Figma_Web_Audit.md`) are the first place in this file a status-bearing component's "main tone" resolves through a proper Brand-tier primitive rather than a raw MUI-import or raw material-palette variable. Any future status-bearing component migration — Button included, eventually — should alias onto this existing tier rather than re-deriving it or reaching for a raw swatch again. Button's own `Components/Button/{Status}/BG/Default` tokens were deliberately left un-migrated onto this tier as part of Progress's own pass (out of scope), but they are the natural next candidate.

**Flagged product gaps (design-owned, not engineering leftovers):**
- Drawer has no `anchor` (left/right/top/bottom) or `variant` (temporary/persistent/permanent) variant set in Figma, it's a single `COMPONENT`, not a `COMPONENT_SET`. Needs a dedicated design pass to define the real axes before it can be built, same pattern as Backdrop's initial "structurally thin" flag before its later Style×Visibility pass.
- No real `SwipeableDrawer` Figma master exists; the page's "swipeable" mockup is a hand-composed illustration, not a real component. Pre-existing, unrelated to the Drawer pass.
- **Resolved, 2026-08-07:** the nested `List`/`ListItem`/`ListSubheader` content in two of Drawer's real-world mockups is no longer a gap, List's own token migration closed it automatically (see the List row above and `docs/Drawer_Figma_Web_Audit.md` §8). The nested `<Icon>` internals inside those same instances remain a separate, still-open item, folded into the general Icons/List cross-component note below.
- `<ListSubheader>` is a single COMPONENT with no variant axis (`color`/`disableGutters`/`disableSticky`/`inset` not modeled), and `<ListItem>` carries MUI's pre-v5 button-state responsibility instead of a separate `ListItemButton` master. Both flagged for a future dedicated design pass, not built.
- `Fab.figma.tsx`'s `Color` enum still has no mapping for `Default`, `Inherit`, or `Inherit (white)`. Small, separate follow-up, not part of either FAB gap closed on 2026-08-06.
- SpeedDial's own token migration is real, deferred work (see SpeedDial row above), plus no `MuiSpeedDial*` `brandTheme.ts` entry yet.
- **Open, non-destructive anomaly (2026-08-06):** after the SpeedDial-into-FAB merge, the emptied, renamed origin "Speed Dial" Figma page disappeared entirely from the file's page list (73 pages vs. the previous 74), with no delete call ever issued by Claude Code. Confirmed non-destructive both times it was checked, archived content intact, zero live references, flagged independently in both `FAB_Figma_Web_Audit.md` §9 and `SpeedDial_Figma_Web_Audit.md` §9. Not resolved, root cause unknown; reconfirm next time the Figma file is opened directly.
- `CleanInputs` (Icons) has a real Figma master with no matching web SVG in `public/icons/edge/`; `check.svg`, `heart.svg`, `star.svg` exist on web with no Figma counterpart, likely legacy/test assets. Both flagged for design to decide on cleanup/export, not investigated further.
- **Icons architecture question, flagged not resolved:** whether icons should own a fixed fill color at all (all migrated to `Semantic/Icon/Default`) versus inheriting `currentColor` from context, the more typical icon-system pattern. This pass migrated the token an existing fixed-fill approach points to, it did not revisit whether a fixed fill is the right approach. Longer-term design/architecture work, not engineering leftovers.
- No `Icon.figma.tsx` Code Connect mapping exists. Creating one for 125+ individual icon masters is a separate, larger initiative, not started.
- Alert dark-mode parity was never covered.
- **Link's Focused-state 2px ring stroke** remains a raw, unbound color (`rgba(0, 0.373, 0.8, 1)`), pre-existing on the original 2 Focused variants and now propagated to all 6. Out of scope for the 2026-08-06 pass's token migration (text-color bindings only), flagged as a separate, smaller follow-up.
- **One Link real-world Gallery instance** (the salvaged header-template demo) carries its own instance-level text-fill override, bound to raw `text/secondary`, distinct from and untouched by the master's own `Color=Inherit` token rebind. Newly discovered 2026-08-06, flagged not fixed.
- Neither `variant` (Typography scale) nor `component` (polymorphic element swap) has a Figma variant equivalent for Link, both are real MUI `Link` props with no Figma-side counterpart. No `Link.figma.tsx` Code Connect mapping exists either.
- **Cross-page Top Header naming inconsistency (hyphen vs. em dash), surfaced 2026-08-07 during Paper's correction pass, not fixed anywhere yet.** Every page's Top Header background is already correctly bound to `Documentation/Header/BG` (checked all 15 other completed pages, zero affected there — Paper's own literal-background bug was Paper-specific, already fixed). But the frame's own name is inconsistent file-wide between `Top Header - EDGE-DS` (hyphen) and `Top Header — EDGE-DS` (em dash, the form the verified `Link` reference uses): Drawer, Chip, Divider, Backdrop, Breadcrumbs, Pagination, and Link are fully em dash on both their Gallery and Documentation frames; List, Button, and Switch are fully hyphen on both; Dialog, Icons, FAB, Alert, Autocomplete, Badge, and ButtonGroup are mixed (one frame each way, on the same component). Pre-existing, not introduced by Paper — a real cross-page cleanup candidate for its own pass (a naming-only fix, no token/structural risk), not something to lose track of now that it's been surfaced. Paper itself was corrected to em dash on both frames as part of its own header rebuild.
- **7 `action/hover` bindings on `<Skeleton>` instances**, found inside Paper's two real-world mockups during the same correction pass, deliberately left un-migrated: `Semantic/State/Hover` exists but resolves to a solid turquoise tint, nothing like the translucent shimmer `action/hover` actually renders, and forcing it would be a real regression. Belongs to `<Skeleton>`'s own future token pass, not Paper's or engineering leftovers from this one.
- **10 more `action/hover` bindings on `<Skeleton>` instances**, same exact class, found inside Progress's two real-world mockups (2026-08-10). Same reasoning, same disposition: deliberately left un-migrated, belongs to `<Skeleton>`'s own future token pass.
- **No `MuiCircularProgress`/`MuiLinearProgress` `brandTheme.ts` entry exists** (2026-08-10). Real, disclosed future risk: Button's native MUI `loading` prop renders its spinner via MUI's own default `loadingIndicator` styling today, with no override in `MuiButton`'s theme entry. If a `MuiCircularProgress` entry is ever added later, it may cascade into Button's loading spinner unexpectedly — whoever adds that entry must re-check Button's rendered loading state directly, not assume independence. See `docs/Progress_Figma_Web_Audit.md` §6.
- Progress's Linear master has no Figma variant for MUI's real `Query` variant. Not built, disclosed gap.
- **Radio's `Large` size** (46px, all 3 Radio-family masters share this axis on `<Radio>`): a real, fully-built Figma variant with no stock MUI Radio equivalent (MUI ships `small`/`medium` only). Flagged in 3 places (Sizing footnote, Key Props table, `MuiRadio`'s own code comment), not wired into code — same class as Fab's missing color variants, Drawer's missing anchor/variant axis.
- **`<RadioGroup>`'s `row` (horizontal layout) prop** has no matching Figma variant — `<RadioGroup>`'s only real axis is `Radios` (3/5) x `Disabled`. Flagged in the Key Props table, not built.
- **`MuiFormLabel`/`MuiFormHelperText` have no `brandTheme.ts` entries yet** (2026-08-10, surfaced by Radio's pass): the new `Components/Radio/Label/*` tokens belong to `<RadioGroup>`'s composed `<FormLabel>`/`<FormHelperText>`, which have no code home. Deferred, same class of exclusion as Autocomplete's Select/MenuItem/ListSubheader debt.
- **Second occurrence of the Brand-tier neutral-primitive gap** (2026-08-10): Radio's `Components/Radio/Icon/Default` is a new literal rather than an alias, for the same reason Autocomplete's `Semantic/Icon/Default` originally was — no existing Brand-tier neutral primitive exists to alias a translucent-black-at-60%-opacity icon/dot role against. Flagged again as a candidate for a future Semantic-tier consolidation pass (would fold both occurrences into proper aliases at once), not blocking either component.
- **Rating's `Active=False, Hovered=true` gap** (2026-08-11): missing for all 3 `<Star>` sizes (3 of 18 possible variants) — a real, confirmed Figma gap, not fabricated. Flagged in the Documentation frame's Sizing footnote and in `docs/Rating_Figma_Web_Audit.md`. A design-library decision, not an engineering leftover.
- **`<Rating>` has no `readOnly`/`precision`/`max` variant modeling** (all are runtime-only MUI props with no Figma variant equivalent) **and no `icon`/`emptyIcon` instance-swap property** — both flagged in Key Props, not built. Same class of exclusion as Checkbox's own `icon`/`checkedIcon`/`indeterminateIcon` disclosure.
- **Rating's cross-page Icons dependency, left untouched, worth remembering** (2026-08-11): `StarSharp`/`StarHalfFilled`/`StarOutlineFilled` on the Icons page are Rating-exclusive by naming convention despite living physically on another page, per Icons' own precedent of excluding non-generically-tinted icons from its own migration. Any future Icons-page pass should know these three are already correctly bound to `Semantic/Icon/Default` and belong conceptually to Rating, not to Icons' own remaining scope.
- **Typography's `table-label` variant (formerly named `custom`) has no `edgeTypography` key yet** (2026-08-11) — deliberately deferred, not a bug. Its binding to the `table/header` Figma style was investigated and confirmed correct (436 file-wide consumers, canonical use on the Table page), so the variant was renamed rather than rebound. No web token was added because EDGE-DS has no Data Grid or Table implementation in code yet to anchor one to — revisit once either gets built. Same class of deferral as `h2`/`h4`/`h6`/`subtitle1`.
- **A `getInstancesAsync()` undercount, not just an overcount, hit this same pass** (2026-08-11): the original Typography discovery recorded 3 real instances of the `custom`/`table-label` variant on the Data Grid page; a closer look during the naming investigation found 36 — the same page-load-session-scoping issue already noted below, but this time undercounting by ~12x rather than transiently overcounting. The other 120 of Typography's originally-reported 121 total instances were never independently re-audited variant-by-variant, so treat that figure as a probable floor, not a confirmed ceiling.
- **Button, Chip, and Checkbox's own doc-chrome text is only 41-81% bound to real Text Styles** (2026-08-11, surfaced by Typography's text-tokenization pass): all three share the identical two structural gaps Typography just fixed for itself (the header wordmark, dense-table/matrix content) — untouched, since retrofitting 3 already-completed components was out of scope for a single-component pass. Strong candidate for a dedicated file-wide sweep, same pattern as the 2026-08-07 documentation-chrome color sweep (7 pages in one pass) — now that `Documentation/Wordmark`, `Documentation/Table/Header`, and `Documentation/Table/Body` already exist from Typography's own pass, applying them elsewhere should be mostly mechanical.
- **`h2`, `h4`, `h6`, `subtitle1` have no `edgeTypography` mapping at all** and fall through to MUI's stock defaults (2026-08-11) — unlike `h1`/`h3`/`h5`/`body1`/`body2`/`caption`, which now all resolve through the EDGE scale. No comparable "prior mapping pointed one tier off" signal was found for these the way it was for h1/h3, so this wasn't investigated further this pass; would need its own dedicated look.
- **`DocUI.tsx` (the shared wrapper most styleguide pages render through) and `foundations/palette/page.tsx` bypass the typography token system entirely** with hardcoded `sx` font literals that duplicate — sometimes inexactly — `edgeTypography` values (2026-08-11). A usage-pattern cleanup for a future pass, not a token-correctness issue caught by this one.
- **Pre-existing internal tile overlap** between `<Typography>`'s `h1, Gutter Bottom=True`/`False` variants in the Master Component Set's own source layout (2026-08-11) — confirmed real via each variant's own local coordinates, not introduced by this pass's reparent, not fixed. A Figma-side layout fix for whoever owns that page next.
- **5 of Typography's 121 real instances resolve to a `null` containing page** (2026-08-11) — orphaned or off-canvas remnants, not chased down.
- **`Semantic/Status/Success/Border` is used for the "Migrated ✓" badge's text color** (2026-08-11, Typography's correction pass) — an exact hex match (`4CAF50`), but the token's own name implies border use, not text use. Disclosed, not renamed — a candidate for a future token-hygiene pass adding a properly-named `Semantic/Status/Success/Text`.
- **Documentation section-label color isn't consistent file-wide** (2026-08-11, surfaced by Typography's Button/Chip/Checkbox comparison): Button and Checkbox use teal (`Brand/Primary/500`) for prescriptive sections like "Usage Guidelines" vs. muted-gray for preview sections; Typography uses muted-gray uniformly; Chip doesn't clearly show the same split at all. Not aligned, since the reference set itself isn't consistent on this pattern — flagged for whenever this gets a deliberate look, not assumed to have one correct answer yet.

**2026-08-07 cross-page documentation-chrome token sweep:** a 2026-08-03 fix had already solved raw/unbound chrome colors (card backgrounds, borders, captions, body text) at the root, in the `Component Doc - EDGE-DS` master template, but that fix never reached pages cloned from already-drifted lineage. Swept Chip, Dialog, Divider, Drawer, FAB (+SpeedDial), Icons, and Link, applying the same already-accepted token mapping to all seven (`Semantic/Surface/Paper`, `Documentation/Surface/Page`, `Documentation/Border/Subtle`, `Semantic/Text/Primary`, `Semantic/Text/Tertiary`, `Documentation/Text/Muted`, `Brand/Primary/500`). All real master components across all 7 pages reconfirmed fully clean, zero regressions, this was documentation-chrome only. Also resolved in the same pass: a stale-`.name`, correct-`.characters` false read on a "Component Placeholder Slot" node that looked like duplicated Checkbox prose across 5 pages, turned out to be a non-issue on every page checked. Per-page detail in each component's own audit doc's newest dated section (Chip's is in `Chip_Context_Progress_Report.md` §8, Dialog's and Divider's are brand-new files since neither had one before).

Not started yet, mentioned as candidates in various sessions: Menu (visible in Figma's Navigation & Structure group, not yet built), and continuing the "Navigation & Structure" page group. (The native Figma variant set for Backdrop, previously listed here as not-started, is done, see the Backdrop row above.)

## 5. Current git / working-tree state (as of 2026-08-06, verified directly)

- `main` is at `5fe7b05` locally (Icons), 3 commits ahead of `origin/main`, **not pushed**. Link's own work since then is uncommitted: modified `EDGE-DS_Claude_Account_Handoff.md`, `src/theme/brandTheme.ts` (new `MuiLink` entry); untracked `docs/Link_Figma_Web_Audit.md`, `.claude/` (exclude from any commit, local tool config, not project content).
- **A fifth stale-looking `.git/index.lock` was hit 2026-08-06 around 17:48**, 0 bytes, appeared during this session's own verification pass again (same pattern as the fourth one during Icons). Same caution as before: check for a real running process before deleting, this doc's editor has no visibility into your actual Mac's processes.
- No `src/app/styleguide/drawer/page.tsx`, `speedDial/page.tsx`, `icons/page.tsx`, or `link/page.tsx` rebuild, nor updates to `src/app/styleguide/fab/page.tsx`, exist, by design, see the process-change note in §3.
- **Resolved, 2026-08-07:** the near-miss during the Link build (Claude Code's script accidentally cloned Link's new Gallery frame onto the Icons page via a `clone()` gotcha, then caught and moved it) is now independently confirmed clean. Igor checked the Icons page's layers panel directly in Figma: exactly the 2 expected top-level frames (`Icons - Documentation`, `Icons - Component Gallery`), nothing stray. No longer an open item.
- **Correction to a prior version of this doc:** the note under Backdrop above (previously "Figma master component itself is still a raw, un-adapted MUI import with zero variants") was accurate as of 2026-07-23 but is now stale, both the Figma variant set and the web `brandTheme.ts` wiring are done as of `1a81a16`. Docs and handoff notes in this project have repeatedly drifted a few days behind the actual commit history, when in doubt, check `git log` directly rather than trusting a status table's date.
- Note for future sessions: this project has now hit stale/ambiguous `.git/*.lock` files five times (2026-07-21 x2, 2026-08-06 x3). A multi-day-old lock has always turned out to be safe to remove; a same-session, minutes-old lock has not yet been definitively confirmed either way, always check for a real running process first regardless of how the prior ones turned out.

## 6. Known environment gotchas worth not re-learning

- **A benign harness event can get hallucinated into a fake security incident once a session is already primed by a real one.** 2026-08-07: a Chip-page subagent first genuinely hallucinated an injected "system-reminder" (quoted text that existed nowhere in its own transcript, caught by an internal date inconsistency and retracted). Minutes later, in the same session, a routine, content-free `date_change` system attachment fired (just a session-crossed-midnight marker, no text, no instruction). The model pattern-matched this unrelated benign event onto its own earlier debunked narrative, fabricated matching quoted "instructions" that never existed, and then repeated the fabrication as settled fact in its summary to Igor. Investigated and confirmed neither event was a real injection. Lesson: once a session has flagged one injection scare, be more skeptical, not less, of a second similar claim from the same session, and always ask for the literal quoted source before trusting a security-flavored claim at face value, even from a model that seems to be doing the right transparency thing by disclosing it.
- **Likely explanation for the repeated `.git/index.lock` sightings (2026-08-06/07, five occurrences):** every single one has appeared within seconds of the Cowork session's own `git status`/`git log`/`git diff` checks run through its sandboxed device bridge, which cannot delete files (a separate, known mount restriction), so any lock its own git commands create never gets cleaned up from that side. Claude Code's own terminal (on the real Mac) has confirmed "no real process" and cleared it cleanly every single time. Working theory, not airtight, but strong enough that future occurrences during a Cowork verification pass probably don't need fresh alarm, still worth a quick real-process check before deleting, just cheaper to do without re-deriving the uncertainty each time.
- **Figma Plugin API / MCP bridge:** editing a live INSTANCE's plain-frame (non-slot) children fails ("Cannot move node..."); fix is `detachInstance()` first. Cloning an instance nested inside another live instance can corrupt node-ID resolution; fix is to detach immediately after cloning and hold object references rather than re-fetching by ID. `figma.createAutoLayout()` throws in this bridge; use the manual `createFrame` + set `layoutMode` recipe instead. Scripts are not atomic, a failure partway through can leave orphaned nodes; always re-screenshot and recheck `page.children` before calling a build done.
- **Local build sandbox:** `next build`/`next dev` cannot run directly against the FUSE-mounted project path in Claude's sandbox (Turbopack `EPERM` on `.next`). Workaround used previously: `rsync` the repo to a real local path (e.g. `/tmp/edge-ds-build`) and build there. Not an issue on your actual machine.
- **Master template:** never delete-and-replace the shared master component's children to add structure, it silently wipes every other component's instance overrides file-wide (happened once, recovered via Figma version history). Append-only edits are safe.
- **`getInstancesAsync()` can return an inflated or transient count when read immediately around a reparent/move operation** (Typography's correction pass, 2026-08-11): a mid-operation read returned 4,284 against a steady, twice-verified 121 read before and after settling. Don't trust or report a count taken mid-operation; always take the read once the operation has fully settled, and re-verify rather than assume the bigger number is the real one.
- **A build's own "3 consecutive clean checks" can still miss real problems a visual review catches** (Typography, 2026-08-11): a stray 3rd top-level frame, a placeholder logo, a grid overlap, and ~17 un-tokenized raw hex fills all passed the build's own automated checks and were only caught when Igor looked at an actual screenshot. Automated checks verify what they're written to check for, not general correctness — a real visual pass before commit is still worth doing on any component with a non-trivial Gallery/Documentation rebuild, not just a nice-to-have.

Full blow-by-blow detail for any of the above lives in this session's memory files if you want the complete history, but the summary above is what matters for continuing the work.

## 7. Figma access for the corporate account

Your new Figma personal access token (for setting up the Figma connector / MCP integration on the corporate account) was shared out-of-band and has since been rotated — it is intentionally not reproduced here.

Notes:
- The repo's own `.env` already has a `FIGMA_ACCESS_TOKEN` entry (used by the Next.js app, not by Claude's tools), and `.env`/`.env*.local` are already gitignored — any new token does not need to touch git either way.
- Treat this class of key as live and sensitive: never paste it into any committed file, Slack message, or shared doc.
- If you're using the `figma-console` Desktop Bridge plugin (the one used throughout this project for direct Plugin-API automation), that connects via the Figma desktop app's plugin session, not this token, no action needed there beyond opening the file with the plugin running.

## 8. Suggested first message in the new corporate session

> "This is the EDGE Design System (EDGE-DS) project, migrated from my personal Claude account. Read `EDGE-DS_Account_Migration_Handoff.md` in the project root for full context: stack, documentation standards, component status, and open items. [Then state whatever you want to work on next.]"
